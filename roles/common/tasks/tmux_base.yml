- name: Make directory for byobu conf
  file:
    path: ~/.byobu
    state: directory

- name: Make directory for tmux.conf
  file:
    path: ~/.config/tmux
    state: directory

- name: Clone tpm
  git:
    repo: https://github.com/tmux-plugins/tpm
    dest: ~/.config/tmux/plugins/tpm

- name: Copy base.tmux.conf
  copy:
    src: files/tmux/config/base.tmux.conf
    dest: ~/.config/tmux/base.tmux.conf

- name: Copy .tmux.conf
  copy:
    src: files/tmux/.tmux.conf
    dest: ~/.tmux.conf

- name: Copy .tmux.conf for byobu
  copy:
    src: files/tmux/.tmux.conf
    dest: ~/.byobu/.tmux.conf
